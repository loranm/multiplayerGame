html
  head
    title
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootswatch/3.3.6/superhero/bootstrap.min.css')
    script(src='/socket.io/socket.io.js')
    script(src='//code.jquery.com/jquery-1.11.3.min.js')
    script.
      var socket = io();
      function sendFunction() {
        if($('#new-message').val()){
          socket.emit('new message', $('#new-message').val());
          $('#new-message').val('');
        }
      };
          
      socket.on('chat message', function(msg){
      $('#messages-area').append($('<li>').text(msg));
      });

  body
    .container-fluid
      .page-header
        h1
      .row
        .col-sm-12
          ul#messages-area.list-group
    nav.navbar.navbar-default.navbar-fixed-bottom
      .container-fluid
        form#form.navbar-form(onsubmit='return false;')
          .form-group(style='width:83%')
            input#new-message.form-control(type='text', placeholder='Search', style='width:100%')
          button.btn.ban-primary(type='submit', onclick='sendFunction()', style='width:16%') Send
    script.
      var socket = io();
      
      console.log(socket);
